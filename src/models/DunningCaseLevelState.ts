'use strict';

enum DunningCaseLevelState{

    INITIALIZING = 'INITIALIZING',
    PENDING = 'PENDING',
    FAILED = 'FAILED',
    CANCELED = 'CANCELED',
    SUCCEEDED = 'SUCCEEDED',
}

export { DunningCaseLevelState }
