'use strict';

enum SpaceReferenceState{

    RESTRICTED_ACTIVE = 'RESTRICTED_ACTIVE',
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
    DELETING = 'DELETING',
    DELETED = 'DELETED',
}

export { SpaceReferenceState }
