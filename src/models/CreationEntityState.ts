'use strict';

enum CreationEntityState{

    CREATE = 'CREATE',
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
    DELETING = 'DELETING',
    DELETED = 'DELETED',
}

export { CreationEntityState }
