'use strict';

enum PaymentTerminalConfigurationState{

    CREATE = 'CREATE',
    ACTIVE = 'ACTIVE',
    DELETING = 'DELETING',
    DELETED = 'DELETED',
}

export { PaymentTerminalConfigurationState }
